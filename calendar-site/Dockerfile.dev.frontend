# Development frontend - uses Vite dev server with hot reload
FROM node:18-alpine

WORKDIR /app
COPY frontend/package*.json ./
RUN npm ci
COPY frontend/ ./

EXPOSE 5173

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "5173"]
